# legacy/iRecHS2

This directory restores the latest pre-deletion `iRecHS2` working tree from the original `BehaviorBox` history.

It contains:

- legacy MATLAB client code
- legacy Python client/support scripts
- legacy test files
- legacy documentation and assets
- the current legacy Windows executable `iRecHS2.exe`
